import React from 'react';
import './Header.css'; // Optional: create a CSS file for Header styles

const Header = () => {
  return (
    <header className="header">
      <h1>세명컴고 TEMP HEADER</h1>
    </header>
  );
};

export default Header;
